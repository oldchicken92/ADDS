#ifndef CHECKPLAYERLOSS_H
#define CHECKPLAYERLOSS_H
#include "Player.h"
#include <vector>

class checkPlayerLoss{
    public:
    bool weakness_check(Move* player1, Move* player2); //checks to see if player1 is weak/lose against player2
};


#endif